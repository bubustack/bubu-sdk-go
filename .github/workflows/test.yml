name: Tests

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  go-test:
    name: Go 1.26.x on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm64, macos-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          check-latest: true
          cache: true
          cache-dependency-path: go.sum

      - name: Run tests
        run: make test

  race-and-coverage:
    name: Race and coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          check-latest: true
          cache: true
          cache-dependency-path: go.sum

      - name: Run race detector with coverage
        run: go test -race -coverprofile=coverage.out ./...

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v7
        with:
          name: coverage.out
          path: coverage.out

  govulncheck:
    name: Govulncheck
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          check-latest: true
          cache: true
          cache-dependency-path: go.sum

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: |
          "$(go env GOPATH)/bin/govulncheck" ./...

  api-compat:
    name: API compatibility
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          check-latest: true
          cache: true
          cache-dependency-path: go.sum

      - name: Install gorelease
        run: go install golang.org/x/exp/cmd/gorelease@latest

      - name: Run API compatibility check
        run: |
          git fetch --tags --force
          if git tag --list 'v*' | grep -q .; then
            "$(go env GOPATH)/bin/gorelease"
          else
            echo "No release tags found; skipping API compatibility bootstrap check."
          fi

  envtest:
    name: Envtest ${{ matrix.k8s-version }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs: go-test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm64]
        k8s-version: ['1.26.x', '1.27.x', '1.28.x', '1.29.x', '1.30.x', '1.31.x']
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.x'
          check-latest: true
          cache: true
          cache-dependency-path: go.sum

      - name: Setup envtest assets
        run: |
          go install sigs.k8s.io/controller-runtime/tools/setup-envtest@latest
          ARCH=amd64
          if [[ "${{ matrix.os }}" == *"arm64"* ]]; then
            ARCH=arm64
          fi
          KUBEBUILDER_ASSETS=$($(go env GOPATH)/bin/setup-envtest use -p path --arch "${ARCH}" --os linux ${{ matrix.k8s-version }})
          echo "KUBEBUILDER_ASSETS=$KUBEBUILDER_ASSETS" >> "$GITHUB_ENV"

      - name: Run envtest smoke tests
        run: go test -tags=integration ./integration/...
